<!DOCTYPE html>
<html>
<head>
  <title>Splittr</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content = "minimal-ui, width = device-width, initial-scale = 1.0, minimum-scale = 1, maximum-scale = 1, user-scalable = no" />
  <meta name="apple-mobile-web-app-capable" content = "yes" />
  <meta name="mobile-web-app-capable" content = "yes" />
  <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Bungee+Inline" rel="stylesheet">
</head>
<body>
<nav>
  <div class="container">
    <div class="row">
      <div class="col col-4">
        <span class="logo"><%= link_to 'Splittr', root_path %></span>
      </div>
      <div class="col col-8 right">
        <%= link_to 'New Bill', cart_path(@current_cart) %> |
        <%= link_to 'Pay Bill', login_path unless @current_user %>
        <%= link_to 'Logout', logout_path, method: :delete if @current_user %>
      </div>
    </div>
  </div>
</nav>

<div id="app">
  <%= yield %>
</div>

</body>
</html>
