<h1>User: <%= @user.name %></h1>

<ul>
<% @user.bills.each do |bill| %>
  <li>
    <%= "Location: #{bill.location} | Date: #{bill.date}" %> |
    <%= link_to 'Show', bill_path(bill) %>
  </li>
<% end  %>
</ul>

<%= link_to 'Logout', logout_path, method: :delete if @current_user %>
